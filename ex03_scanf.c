#include <stdio.h>

int main()
{
  int a;

  scanf("%d", &a);  //scanf prende input dall'utente - nella stringa non si può mettere altro oltre il %lettera - va messo & prima della variabile

printf("il tuo numero è: %d\n", a);

}